{
  "installCommand": "corepack enable && corepack prepare pnpm@10.24.0 --activate && pnpm install",
  "buildCommand": "pnpm --filter @omnilp/shared build && pnpm --filter @omnilp/web build",
  "outputDirectory": "web/.next",
  "framework": "nextjs"
}
